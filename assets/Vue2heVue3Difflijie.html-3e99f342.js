import{_ as e,o as d,c as o,e as a}from"./app-2dc9ef31.js";const i={},n=a('<h2 id="vnode" tabindex="-1"><a class="header-anchor" href="#vnode" aria-hidden="true">#</a> Vnode</h2><p>在vue中，会根据真实dom结构来模拟出一份数据(Vnode)，在一段时间内，对于dom的操作都会先在Vnode上体现，最终我们会得到两份数据，即new Vnode和 old Vnode。</p><h2 id="diff" tabindex="-1"><a class="header-anchor" href="#diff" aria-hidden="true">#</a> Diff</h2><p>将<code>new Vnode</code>和<code>old Vnode</code>进行对比，争取尽可能多的复用节点，以争取用最小的更新代价来渲染出新的页面。 说起diff算法，个人认为它们和奥数倒有些相似，对于一些满足特殊规则的片段，能用特殊的方法快速得到结果。所以react diff、vue2 diff、vue3 diff它们都是在各自的特殊处理上有所不同罢了，但殊途同归，它们都是为了解决能更快速的完成对比并且更多的复用。</p><h2 id="key" tabindex="-1"><a class="header-anchor" href="#key" aria-hidden="true">#</a> Key</h2><p>既然有diff对比，那么它必然需要一个唯一的键来作为标识，这便是我们经常提到的<code>key</code>了。</p><h2 id="diff总是有必要吗" tabindex="-1"><a class="header-anchor" href="#diff总是有必要吗" aria-hidden="true">#</a> Diff总是有必要吗？</h2><p>diff存在的动机肯定是要优化渲染性能（快速的比对以及尽可能的复用节点），这是毋庸置疑的，但是，它总是会起优化作用吗？或者说，在什么情况下，对比新旧Vnode这个过程显的没有意义？</p><p>例子一： <code>old Vnode</code>: [ 1, 2, 3, 4, 5, 6, 7, 8 ] <code>new Vnode</code>: [ 1, 2, 5, 4, 3, 6, 7, 8 ] 通过比对，发现只是 &quot;3&quot;和&quot;5&quot;的位置调换了，那么我们只需要去操作&quot;3&quot;和&quot;5&quot;就可以了。</p><p>例子二： <code>old Vnode</code>: [ 1,2,3,4,5,6,7,8 ] <code>new Vnode</code>: [ 10,11,12,.....] 通过比对，我们会对1做删除操作，10做新增操作，以此类推...,（当然这个过程也会是借助文档碎片最终批量完成）</p><p>但是我们通过肉眼一下就能看出，new Vnode全是新增，old Vnode全是删除，根本不需要进行比对。省下比对这个时间不香吗（vue3借助编译器对此做了优化，很nice）。其实在vue2中，当父级发生改变后，便不会去对比子集了，会直接用新的，由此也能看出，避免无意义的比较，节省diff消耗的过程是很有价值的。</p><h2 id="总结" tabindex="-1"><a class="header-anchor" href="#总结" aria-hidden="true">#</a> 总结</h2><p>Vue 2.x使用的是双向指针遍历的算法，也就是通过逐层比对新旧虚拟DOM树节点的方式来计算出更新需要做的最小操作集合。但这种算法的缺点是，由于遍历是从左到右、从上到下进行的，当发生节点删除或移动时，会导致其它节点位置的计算出现错误，因此会造成大量无效的重新渲染。 Vue 3.x使用了经过优化的单向遍历算法，也就是只扫描新虚拟DOM树上的节点，判断是否需要更新，跳过不需要更新的节点，进一步减少了不必要的操作。此外，在虚拟DOM创建后，Vue 3会缓存虚拟DOM节点的描述信息，以便于复用，这也会带来性能上的优势。同时，Vue 3还引入了静态提升技术，在编译时将一些静态的节点及其子节点预先处理成HTML字符串，大大提升了渲染性能。 因此，总体来说，Vue 3相对于Vue 2拥有更高效、更智能的diff算法，能够更好地避免不必要的操作，并提高了渲染性能。 除了在策略上的变化外，vue2 diff是全程用的数组，vue3 diff 在过程中借助了map，从数据结构上，map的读取速度也更快。</p>',13),f=[n];function c(t,r){return d(),o("div",null,f)}const u=e(i,[["render",c],["__file","Vue2heVue3Difflijie.html.vue"]]);export{u as default};
